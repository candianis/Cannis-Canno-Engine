#pragma once

#include "ccpch.h"
#include "Core/Core.h"

namespace Cannis {
	class CANNIS_API LayerStack {
	//public:
	//	LayerStack();
	//	~LayerStack();

	//	// @brief Pushes the layer to the front of the stack
	//	// @param p_layer layer to be placed at the front of the stack
	//	void PushLayer(Layer* p_layer);

	//	// @brief Pushes the layer to the end of the stack
	//	// @param p_overlay layer to be added to the end of the stack
	//	void PushOverlay(Layer* p_overlay);

	//	// @brief Deletes the layer 
	//	// @param p_layer Layer to be searched in the stack for deletion
	//	void PopLayer(Layer* p_layer);

	//	//@brief 
	//	void PopOverlay(Layer* p_overlay);

	//	// @returns The first iterator of the stack
	//	std::vector<Layer*>::iterator begin() { return m_layers.begin(); }

	//	// @returns The last iterator of the stack
	//	std::vector<Layer*>::iterator end() { return m_layers.end(); }

	//private:
	//	std::vector<Layer*> m_layers;
	//	std::vector<Layer*>::iterator m_layerInsert;
	};
}